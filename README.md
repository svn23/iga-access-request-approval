# iga-access-request-approval

Access request and approval workflow for Evolveum midPoint (IGA).

## Status

Early development. Work happens on `dev-*` branches and merges into `main`.

## Layout

- `midpoint/` — midPoint configuration objects (roles, policies, approval workflows)
